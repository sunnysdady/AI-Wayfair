# 广告周期上下文审计（2026-07-22）

## 结论

1. 广告管理器不再强制展示“成熟周（推荐）”，默认使用“最近 7 天”。
2. 手动优化保留可操作的周期筛选，因为所选周期会直接驱动实际花费和诊断数据；移除了固定历史日期说明。
3. “成熟周 / T-14”仅保留在 AI 优化工作台，作为建议与 API 动作的决策证据，不再挂载到普通报表页面。
4. 无周期页面不再显示空的“- 至 -”日期占位。

## 证据

- `01-ad-manager.png`：广告管理器从“最近 7 天”开始，无成熟周入口与成熟周状态文案。
- `02-manual-todo.png`：手动优化仍可选择业务周期，无固定历史恢复文案。
- `03-ai-optimization.png`：成熟周与 T-14 仅出现在 AI 决策工作台。

## 验证

- `npm test`：87/87 通过。
- `npm run lint`：通过。
- `git diff --check`：通过。
