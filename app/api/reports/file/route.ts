async function bindings(){return (await import("cloudflare:workers")).env;}

export async function GET(request:Request){try{const env=await bindings();const id=new URL(request.url).searchParams.get("id");if(!id)return Response.json({error:"缺少报告id"},{status:400});const row=await env.DB.prepare("SELECT file_name,content_type,object_key FROM report_uploads WHERE id=?").bind(id).first<{file_name:string;content_type:string;object_key:string}>();if(!row)return Response.json({error:"报告不存在"},{status:404});const object=await env.FILES.get(row.object_key);if(!object)return Response.json({error:"报告文件不存在"},{status:404});const headers=new Headers();headers.set("content-type",row.content_type);headers.set("content-disposition",`inline; filename*=UTF-8''${encodeURIComponent(row.file_name)}`);headers.set("x-content-type-options","nosniff");if(row.content_type==="text/html")headers.set("content-security-policy","default-src 'none'; style-src 'unsafe-inline'; img-src data: https:; font-src data:; base-uri 'none'; form-action 'none'; frame-ancestors 'self'");return new Response(object.body,{headers});}catch(error){return Response.json({error:error instanceof Error?error.message:"报告文件读取失败"},{status:500});}}
