# Claude / Anthropic is the default agent route for this project.
# Copy to .env locally if needed. Never commit real secrets.

ANTHROPIC_AUTH_TOKEN=

# Optional: local proxy / gateway used in China network environments.
ANTHROPIC_BASE_URL=http://127.0.0.1:15721

# Reasonix / DeepSeek is intentionally not the default route.
# DEEPSEEK_API_KEY=
